<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>profile page</title>
  </head>
  <body>
    <div class="flex justify-end w-full bg-zinc-800 p-2">
      <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm"
        >Logout</a
      >
    </div>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h3 class="text-3xl mb-5 mt-10">hello, <%= user.name %></h3>
      <h5 class="mb-5">You can create a new post.</h5>
      <form action="/post" method="post" class="flex flex-col mb-5">
        <textarea
          name="content"
          class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md mb-5"
          placeholder="write your post here"
        ></textarea>
        <input
          type="submit"
          class="w-1/3 px-3 py-2 bg-blue-500 rounded-md text-xl"
          value="create new post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Post.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach((post)=>{ %>

            <div class="post w-1/3 mb-4 p-4 border-2 border-zinc-700 bg-zinc-800 rounded-md">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                <p class="text-sm tracking-tight">
                  <%= post.content %>
                </p>
                <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                <div class="btns flex gap-5 mt-2">
                    <a href="/like/<%=post._id %>" class="text-blue-500">
                       <%= post.likes.indexOf(user._id) === -1 ? "like" : "Unlike" %>
                    </a>
                    <a href="/edit/<%= post._id %>" class="text-zinc-400">Edit</a>
                </div>
              </div>
         <% }) %>

        </div>
      </div>
    </div>
  </body>
</html>
