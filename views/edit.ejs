<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>profile page</title>
  </head>
  <body>
    <div class="flex justify-end w-full bg-zinc-800 p-2">
      <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm"
        >Logout</a>
    </div>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h5 class="mb-5">Edit Your post.</h5>
      <form action="/update/<%= post._id %>" method="post" class="flex flex-col mb-5">
        <textarea
          name="content"
          class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md mb-5"
          placeholder="write your post here"
        ><%= post.content %></textarea>
        <input
          type="submit"
          class="w-1/3 px-3 py-2 bg-yellow-500 text-black rounded-md text-xl"
          value="Update Your post"
        />
      </form>
    </div>
  </body>
</html>
